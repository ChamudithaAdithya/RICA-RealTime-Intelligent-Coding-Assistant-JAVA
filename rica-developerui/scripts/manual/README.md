# Manual utilities

These scripts are investigation and development helpers, not part of the installed extension.

Run `npm run compile` before using a JavaScript helper because the helpers import compiled modules from `dist/`.

Large generated inspection output belongs in `artifacts/debug/` and packaged extensions belong in `releases/`.
